import React from 'react';

import makingImg from '../../assets/control.png'

const MakingLives = () => {
    return (
        <div className='w-full h-[500px] bg-cover bg-center mx-auto'
        style={{
            backgroundImage:`url(${makingImg})`
            
        }}
        >
            hello
        </div>
    );
};

export default MakingLives;